# Database Connection (Leave blank or omit when running in in-memory mode)
MONGODB_URI=

# In-Memory Database Mode (Set to true to run locally without a persistent MongoDB Atlas cluster)
USE_MEMORY_DB=true

# Session Secret Key
SESSION_SECRET=your_super_secret_session_key_here

# Server Port
PORT=3001

# Environment (development | production)
NODE_ENV=development
